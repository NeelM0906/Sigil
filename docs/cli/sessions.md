---
summary: "CLI reference for `sigil sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `sigil sessions`

List stored conversation sessions.

```bash
sigil sessions
sigil sessions --active 120
sigil sessions --json
```

